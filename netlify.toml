[build]
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  conditions = {Role = ["admin"]}

# Ensure all paths work correctly
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
